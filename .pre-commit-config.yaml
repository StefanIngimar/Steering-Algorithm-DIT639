repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v20.1.0
    hooks:
      - id: clang-format
        args: [--style=file, -i]
        files: \.(c|cpp|cxx|cc|h|hpp|hxx)$
        exclude: \.(build|external)/
